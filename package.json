{
  "name": "matrix-ai",
  "version": "0.1.0-pre.1",
  "description": "Pre-release/experimental: MiniMax-backed CLI for generation plus structured equity-oriented audit traces (not production-certified).",
  "private": true,
  "type": "module",
  "scripts": {
    "probe": "bun run src/cli.ts",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
